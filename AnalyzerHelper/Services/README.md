# Services

**Purpose:** Loading data and providing rule/report registry to the UI.

- **SolutionLoader** – Loads .xaml workflow files from a solution folder path.
- **RoleFixRegistry** – Provides rule lists for No interaction / Need interaction tabs and report; resolves rules by id for Apply fix / Run rules.

Edit here when you change how solutions are loaded or how the app discovers rules for the tabs.
